# DSF Benchmarks

This directory contains benchmark scripts and outputs for validating DSF against legacy or reference implementations.

Benchmarks are not part of the normal unit-test suite. Unit tests live under `tests/`.
